ANTHROPIC_API_KEY=sk-ant-...

# Image generation — use ONE of the following (Imagen 3 preferred):
GOOGLE_API_KEY=...        # Preferred: Imagen 3 via Google AI Studio (aistudio.google.com)
OPENAI_API_KEY=sk-...     # Fallback: DALL-E 3 — only if no Google key

LINKEDIN_ACCESS_TOKEN=    # Not used: LinkedIn blocks API writes to personal profiles
LINKEDIN_PERSON_URN=      # Not used
